# WireGuard VPN Mesh Configuration
# Generated by messhy
#
# 💡 Either:
#   1. Edit this file manually with your server IPs
#   2. Use Terraform to auto-generate this file
#
# See: config/mesh.example.yml for full examples

shared: &shared
  user: ubuntu
  ssh_key: ~/.ssh/id_rsa
  listen_port: 51820
  verify_host_key: true

development:
  <<: *shared
  nodes:
    dev1:
      host: localhost
      # ssh_port: 2222            # Optional per-node SSH port
      # ssh_user: deploy          # Optional per-node SSH user
      # listen_port: 51821        # Optional per-node WireGuard port
      private_ip: 10.8.0.10
      label: local

production:
  <<: *shared
  nodes:
    # TODO: Add your servers here
    # web1:
    #   host: YOUR_SERVER_PUBLIC_IP
    #   private_ip: 10.8.0.10
    #   label: us-east-1
